# CI Policy

## Required Checks

- Formatting and linting must pass.
- Tests must pass.
- Security checks must pass when configured.

## Merge Policy

- No direct pushes to `main`.
- Pull requests must target `develop`.
- Maintainer approval is required for merge.
